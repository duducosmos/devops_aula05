name: Construindo e Testando

on:
    push:
        branches:
            - main

jobs:
    build-and-test:
        runs-on: ubuntu-latest
        steps:
            - name: primeiro-passo
              run: echo ls
